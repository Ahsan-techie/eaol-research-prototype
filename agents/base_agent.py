"""
Base agent class defining  Agent = <state, policy, tools, confidence-estimator>.
implements bid() for all specialist agents and declares generate_plan(), execute(), and replan() as abstract methods.
"""
